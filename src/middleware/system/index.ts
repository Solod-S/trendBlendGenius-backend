import { CorsMiddleware } from './corsMiddleware.middleware';
import { LoggerMiddleware } from './logger.middleware';

// export const SystemMiddlewares = [CorsMiddleware, LoggerMiddleware];
export { CorsMiddleware, LoggerMiddleware };
